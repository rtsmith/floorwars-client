apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/rtsmith/floorwars:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/rtsmith/floorwars:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: floorwars
        chartPath: charts/floorwars
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/rtsmith/floorwars"
          image.tag: "{{.DIGEST_HEX}}"
